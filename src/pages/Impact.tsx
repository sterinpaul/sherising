
import {
  Navigation,
  // Impact
} from '../components';

const Home = () => {
  return (
    <div className="min-h-screen bg-[color:var(--bg-primary)] text-[color:var(--text-primary)] overflow-x-hidden">
      <Navigation />
      {/* <Impact /> */}
    </div>
  );
};

export default Home;